/*
import * as ecs from "./ecs";

export function create_player(x: number, y: number) {
  let entity = new ecs.Entity();

  entity.components[ecs.TILEPOS] = new ecs.TilePos(x, y);
  entity.components[ecs.DRAWPOS] = new ecs.DrawPos(x, y);
  entity.components[ecs.ANIMATION] = new ecs.Animation("player_none");

  return entity;
}
*/
